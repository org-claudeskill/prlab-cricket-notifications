# cricket-notifications

Push alerts. Sends a notification when a snapshot's last event counted a wicket.

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
pytest
```
